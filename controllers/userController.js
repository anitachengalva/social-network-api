const { ObjectId } = require('mongoose').Types;
const { User, Thought } = require('../models');

module.exports ={
    // get all users
    // get single user
    // post new user
    // put to update user
    // delete to remove user
}